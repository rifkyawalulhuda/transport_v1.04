# 2026-08-05

## 新增：Print A4 Subcontractor 页面添加 DN section

修改 `PrintSubcontractor.vue`：
- 新增 `DNItem` type
- 新增 reactive state: `dnItems`
- 新增 `loadDN()` 函数，调用 `subcontractorService.fetchDNList(id)`，unwrap `data.items`，error diabaikan agar tidak fatal di halaman print
- `onMounted` 并行调用 `loadDetail()` + `loadDN()`
- Template 新增 DN 表格 section（位于 Jadwal Pengiriman 下方，footer 上方），含 9 列：#, No. DN, Pickup, Drop, Qty, Pkg, GW (format angka ID), No. Container, No. AJU
- 新增 CSS：`.dn-block`、`.dn-table`（样式与 Jadwal Pengiriman 表格一致）
- lint 检查无错误

## 修改：Export Excel DN sheet — 将 "ID SC" 列改为 "No. Laporan"

修改 `node_backend/routes/subcontractor.js`：
- 将 sheet DN 中的列 `"ID SC"` (key: `id_sc`) 改为 `"No. Laporan"` (key: `no_laporan`，width 12)
- Loop DN 从 `rows.forEach((row) =>` 改为 `rows.forEach((row, index) =>`
- 值从 `scId`（id_subcontractor）改为 `index + 1`（与 sheet 主表的 "No." 列一致）
- lint 检查无错误
