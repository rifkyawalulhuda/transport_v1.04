The response format is a JSON array of message objects.
You can get lat/lon from messages using the "pos" field, specifically "pos.y" for latitude and "pos.x" for longitude.
The position fields are "y" (latitude), "x" (longitude), "z" (altitude), "s" (speed), "c" (course), and "sc" (stop course).